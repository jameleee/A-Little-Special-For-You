# Paper burn portal — approved user specification

Scope: screen 2 only. Clean travel photo; actual destination page rendered once beneath the white insert. Preserve later page curls.

1. Add configurable background coordinates and transition timing in content JSON.
2. Extract shared screen construction, promote the prepared screen without rerendering.
3. Draw a textured paper cover on canvas; erase an irregular expanding hole, add thin char, flame and ember edges. Match approaches from the right. Fade tabletop while the same portal transforms to the app rectangle.
4. Cancel timers/frames on disposal; lock navigation during transition; use 200ms fade for reduced motion.
5. Run validator, browser check opening/back/replay and mobile layouts; rebuild GitHub Pages archive.
